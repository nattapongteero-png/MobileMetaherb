// ElevenLabs voice config.
// NOTE: a key placed here ships inside the app bundle (extractable) — acceptable
// for a personal demo only. For production, proxy ElevenLabs through a backend
// so the key never reaches the client.
export const ELEVENLABS_API_KEY = ""; // ← paste your ElevenLabs API key here
export const ELEVENLABS_VOICE_ID = "EXAVITQu4vr4xnSDxMaL"; // "Sarah" — female, multilingual (change to your preferred voice)
export const ELEVENLABS_MODEL = "eleven_turbo_v2_5"; // fast + supports Thai
